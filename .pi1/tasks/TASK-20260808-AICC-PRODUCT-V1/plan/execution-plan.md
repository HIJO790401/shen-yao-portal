# AICC 第二核心產品更新｜執行計畫

1. 完整閱讀 Owner 提供的 AICC v0.2.CANDIDATE 母規格，抽取正式名稱、L1／L2／L3、VERSION GAP 與發布閘門。
2. 盤點官網現行產品、履歷、Demo、SEO 與歷史紀錄；保留 SCBKR 主產品以及 TIRC／AVRCK 歷史證據。
3. 將未完成的 Runtime 產品位更新為 AICC OS，讓首頁與產品中心都有單一正式入口。
4. 建立中英文分離的六幕無輸入架構動畫，不提供人類輸入，也不把候選架構宣稱成已上線 Runtime。
5. 將履歷現行第二工程產品由 TIRC 更新為 AICC；TIRC 仍保留在既有 Demo 與工程檔案中。
6. 同步 About、Metadata 與 `llms.txt`，讓搜尋引擎與 AI 能讀到一致的產品名稱及成熟度邊界。
7. 執行型別、Lint、建置、測試、Drizzle、diff 與秘密候選掃描，再以桌機及 390 × 844 手機瀏覽器實機驗收。
8. 只提交本任務相關檔案並直接推送 `origin/main`；不儲存 Sites version、不部署、不改網域。
